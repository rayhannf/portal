university-portal/
│
├── index.html               # ১. হোমপেইজ / ল্যান্ডিং পেইজ
├── login.html               # ২. স্টুডেন্ট লগিন পেইজ
├── dashboard.html           # ৩. মেইন ড্যাশবোর্ড
├── profile.html             # ৪. স্টুডেন্ট প্রোফাইল পেইজ
├── courses.html             # ৫. এনরোল করা কোর্সের তালিকা
├── routine.html             # ৬. ক্লাস রুটিন (JSON থেকে লোড হবে)
├── teachers.html            # ৭. শিক্ষকদের তালিকা (JSON থেকে লোড হবে)
├── notices.html             # ৮. নোটিশ বোর্ড (JSON থেকে লোড হবে)
├── settings.html            # ৯. সেটিংস পেইজ
│
├── components/              # সব পেইজের কমন অংশ (JS দিয়ে লোড হবে)
│   ├── navbar.html          # উপরের ন্যাভিগেশন বার
│   └── footer.html          # নিচের ফুটার অংশ
│
├── css/                     # ডিজাইনের সব স্টাইলশিট
│   ├── style.css            # গ্লোবাল স্টাইল (কালার, বাটন, ফন্ট, হোমপেইজ)
│   ├── dashboard.css        # ড্যাশবোর্ড ও সাইডবারের লেআউট ডিজাইন
│   └── responsive.css       # মোবাইল ও ট্যাবের জন্য মিডিয়া কোয়েরি
│
├── js/                      # প্রজেক্টের সব লজিক ও ফাংশন
│   ├── app.js               # কমন লজিক (Navbar/Footer লোড, লগআউট)
│   ├── auth.js              # লগিন ভ্যালিডেশন (students.json চেক করা)
│   ├── dashboard.js         # ড্যাশবোর্ড সিকিউরিটি ও ইউজার ডেটা দেখানো
│   ├── routine.js           # routine.json থেকে টেবিল তৈরি করার লজিক
│   ├── teachers.js          # teachers.json থেকে কার্ড তৈরি করার লজিক
│   └── notices.js           # notices.json থেকে নোটিশ লোড করার লজিক
│
├── data/                    # আমাদের "ফেক ডাটাবেস" (JSON ফাইলসমূহ)
│   ├── students.json        # লগিন টেস্ট করার জন্য স্টুডেন্টদের আইডি-পাসওয়ার্ড
│   ├── teachers.json        # শিক্ষকদের তথ্য (নাম, ছবি, ডিপার্টমেন্ট)
│   ├── routine.json         # ক্লাস রুটিনের ডেটা
│   └── notices.json         # সব নোটিশের ডেটা
│
└── assets/                  # প্রজেক্টের ছবি ও আইকন
    ├── images/              # ক্যাম্পাস, শিক্ষক বা প্রোফাইলের ছবি
    ├── icons/               # বিভিন্ন আইকন (যদি প্রয়োজন হয়)
    └── logo/                # ইউনিভার্সিটির লোগো